# Andá Launch Page V3

This version fixes the duplicate logo, tagline, button, footer text and social icons.

Upload and replace:

- index.html
- styles.css
- the full assets folder

The hero artwork and footer artwork already contain their text and graphics, so the HTML does not layer duplicate content on top.

Replace each https://example.com link in index.html with your real checkout links.
